{% schema %}
  {
    "name": "Instafeed Section",
    "settings": [],
    "blocks": [
      {
        "type": "instafeed_app_block_V99thA",
        "name": "Instagram Feed",
        "settings": {
          "title": "",
          "title_color": "",
          "alignment": "center",
          "html_area": ""
        }
      }
    ],
    "presets": [
      {
        "name": "Default",
        "category": "Custom"
      }
    ]
  }
  {% endschema %}
  
  <div class="instafeed-section">
    {% for block in section.blocks %}
      {% if block.type == 'instafeed_app_block_V99thA' %}
        {% render 'instafeed', block: block %}
      {% endif %}
    {% endfor %}
  </div>  